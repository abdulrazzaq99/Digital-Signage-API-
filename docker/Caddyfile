# Terminates HTTPS (Let's Encrypt, automatic) and proxies REST and Socket.IO to the API.
{$API_HOST} {
	encode gzip
	reverse_proxy api:4000
}

# Web dashboards (Next.js).
{$WEB_HOST} {
	encode gzip
	reverse_proxy web:3000
}
